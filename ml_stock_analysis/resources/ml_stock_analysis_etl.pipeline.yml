# The main pipeline for ml_stock_analysis

resources:
  pipelines:
    ml_stock_analysis_etl:
      name: ml_stock_analysis_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/ml_stock_analysis_etl"

      libraries:
        - pypi:
            package: wheel==0.41.2
            repo: https://pypi.org/simple/
        - pypi:
            package: pandas==2.2.2
            repo: https://pypi.org/simple/
        - pypi:
            package: numpy==1.26.4
            repo: https://pypi.org/simple/
        - pypi:
            package: yfinance==0.2.36
            repo: https://pypi.org/simple/
        - pypi:
            package: requests==2.32.3
            repo: https://pypi.org/simple/
        - pypi:
            package: flask==3.0.3
            repo: https://pypi.org/simple/
        - pypi:
            package: scikit-learn==1.5.1
            repo: https://pypi.org/simple/
        - glob:
            include: ../src/ml_stock_analysis_etl/transformations/**
        - glob:
            include: ../src/ml_stock_analysis_etl/explorations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          # - --editable ${workspace.file_path}
